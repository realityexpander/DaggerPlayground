package com.example.daggerplayground.car;

import android.util.Log;

import javax.inject.Inject;
import javax.inject.Singleton;

public class Driver {

    // we don't own this clas so we ca't annotate it with @Inject
}
